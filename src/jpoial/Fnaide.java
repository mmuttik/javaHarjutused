/*
package other;

public class Fnaide {

   public static void main (String[] p) {
      TextIO.put ("a = ");
      double a = TextIO.getlnDouble();
      TextIO.put ("b = ");
      double b = TextIO.getlnDouble();
      TextIO.putln ("maksim. = " + String.valueOf (maks (a, b)));
      TextIO.putln ("abs.v. = " + String.valueOf (absVaartus (a)));
   } // main

   public static double maks (double d1, double d2) {
      if (d1 > d2)
         return d1;
      else
         return d2;
   } // maks

   public static double absVaartus (double d) {
      if (d < 0)
         return -d;
      else
         return d;
   } // absVaartus

} // other.Fnaide

*/
